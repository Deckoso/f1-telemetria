[project]
name = "f1tele"
version = "0.3.0"
description = "Gravador de telemetria UDP do F1 25 (jogo no Xbox, PlayStation ou PC)"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
f1tele = "f1tele.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
